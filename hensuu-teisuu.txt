var,let,constの使用について
varは変数に再代入、再宣言が可能。
letは変数に再代入はできるが再代入はできない
constは再代入、再宣言不可

varを使わない理由
varを使うと同じ変数名でいくつも作れてしまうので、別々の値が入っている同じ変数名のものができてしまい管理がしずらくなるため

定数とは
cnost　基本的には再代入、再宣言ができない。中身の値が絶対に変わらないものに使う。中身の値が変わるものにはletを使用する。
スコープ内で代入した定数はスコープ内に限り変更可能。